from functions import square

for i in range(10):
    print(f"The square of {i} is {square(i)}.")

# What if we import only the whole module, and not a part of the module?
#import functions
# for i in range(10):
    #print(f"The square of {i} is {functions.square(i)}.")
